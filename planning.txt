users
    _id             | MongoDB_ID
    firstName       | String
    lastName        | String
    email           | String
    password        | String (hashed?)
    age             | Number
    weight          | Number
    exercises       | String []

workout-sessions
    user            | String (but should tie to MongoDB_ID eventually)
    date            | String
    exercise        | String
    duration        | String
    weight          | Number
    exertion        | Number